# 2026-08-24

## 收尾：新建项目纳入 CJDSL.sln
- 用户指出 A2.1/A3.1 新建项目未加入解决方案。补齐：
  - `CJDSL.Generation`（net10 类库）已注册进 CJDSL.sln，GUID `F6155209-AF0D-4F4C-91DE-F43902F6696B`，归入 `src` 文件夹。
  - `CJDSL.WebComponent`（纯 Node/TS 前端包）按 `DA.DSHPlug.CJDSL.csproj` 模式新建 `CJDSL.WebComponent.csproj`（`EnableDefaultCompileItems/NoneItems=false`，`<None Include>` 纳入 src/**、build.cjs、package.json、tsconfig.json 仅用于解决方案内浏览，不参与 C# 编译），并加入 CJDSL.sln，GUID `4D9D883B-0DEF-43B8-8A29-3EC04EDFE39A`，归入 `src` 文件夹。
- 校验：`dotnet sln CJDSL.sln list` 成功，10 个项目全部识别。
- 注：WebComponent 不做 C# 编译集成，dist 产物由各产品自行引用；是否需要 npm build 接入 dotnet 生成管线待阶段 B 确定。

## 续：CJDSL.React 同样补齐进 sln
- 用户核对发现 `CJDSL.React`（纯 Node/TS React 渲染器，`@cj/cjdsl-react`，build.cjs/esbuild 构建）此前也无 .csproj、未加入 sln。
- 同样按 `DA.DSHPlug.CJDSL.csproj` 模式新建 `CJDSL.React.csproj`（`EnableDefaultCompileItems/NoneItems=false`，`<None Include>` 纳入 src/**、build.cjs、package.json、tsconfig.json，仅浏览不参与 C# 编译），并加入 CJDSL.sln，GUID `E706D40C-DA2B-4386-AB0C-8DB167B2F107`，归入 `src` 文件夹。
- 校验：`dotnet sln CJDSL.sln list` 成功，11 个项目全部识别。
- 决策点：方案「渲染集中化→Web Component 化」指各产品不再自带 React 渲染副本、统一走 WebComponent；但 `CJDSL.React` 作为官方 React 目标平台内核包（@cj/cjdsl-react）保留仍有价值（可复用为内核/备选）。本次仅纳入浏览，未删除任何代码；若后续决定废弃/归档 React 包，由用户定。
