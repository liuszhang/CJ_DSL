# 2026-07-24 工作日志

## Phase 1 整改落地（续上轮会话）

完成 CJDSL 整改方案 Phase 1 全部模块并验证：

- **A 模块（LLM 生成链路 + provider 选择）**：`/api/dsl/generate` 支持 `?provider=template|llm`，解析器 `IDslGeneratorResolver` 按 provider 选择并自动降级。冒烟测试确认两条路径均正确路由到生成器。
- **B 模块（submit/validate/Confirm 处理器）**：`DslEventDispatcher` 改用 MudBlazor 9.6.0 异步 API（`ResetValidationAsync` / `ShowMessageBoxAsync` / `ValidateAsync`）；`HandleClick` 改用页面级 `Context.EventDispatcher`。
- **C 模块（业务 CRUD API + 持久化）**：修复两处运行时缺陷——
  - UTF-8 中文乱码 500：`body.GetRawText()` → `JsonSerializer.Serialize(body)`（BusinessApiEndpoints.cs）。
  - `CS1737` 可选参数必须位于必需参数之后：`/list` 的 `pageIndex`/`pageSize` 调整到 `service`/`ct` 之后。
  - 冒烟测试（save/submit/list/item/delete + 非法 objectCode）全部通过，中文正确往返。
- **D 模块（表单数据绑定）**：`DslComponentRenderer.razor` 所有输入加 `@bind-*:after` 回写 `FormState`；form 在 `OnParametersSet` 登记、`OnAfterRender` 把 `MudForm` ref 登记到 `ComponentRefs`。
- **G 模块（datetime 组件 + Debug.WriteLine 清理）**：datetime 改为 MudDatePicker+MudTimePicker 组合（G3）；删除 chart 分支 Debug.WriteLine（G4）。

## 构建环境重要问题（长期）

- .NET 10 预览版（SDK 10.0.400-preview）在 Windows 下，标准 `obj` 目录被**遗留 dotnet 进程**持续锁定（`rpswa.dswa.cache.json` 与 `*.dll/*.pdb` 写入 Access Denied），影响 CJDSL 与兄弟仓库 CJCore 的 obj。
- 规避办法：`dotnet build CJDSL.sln -c Debug --disable-build-servers --artifacts-path artifacts` 构建到独立目录即可绕开锁。
- 直接 `dotnet run --project src/CJDSL.Web` 因标准 obj 锁会失败；已用 artifacts 输出（拷贝 wwwroot 后）直接 `dotnet artifacts/bin/CJDSL.Web/debug/CJDSL.Web.dll --urls http://localhost:5001` 启动验证。
- 注意：`taskkill` / `Stop-Process` / `tasklist` 在本环境被安全策略拦截（LOLBin），无法杀掉遗留 dotnet 进程；彻底解决需用户在外部终止该进程或重启。

## 构建状态

- 全解方案构建 **0 错误**（14 警告均为既有 MudBlazor 分析器提示与 ASP.NET WithOpenApi 弃用，与本次改动无关）。
- 冒烟测试服务器当前运行于 http://localhost:5001（artifacts 输出）。

## Phase 2 整改落地（本轮）

完成整改方案 Phase 2 全部模块，解决方案（含测试项目）构建 **0 错误**，单测 **10/10 通过**：

- **2.1 E 安全机制**：新建 `IDslSecurityValidator`（Domain/Interfaces）+ `DslSecurityValidator`（Infrastructure/Services）。
  - Jint 沙箱：`opts.TimeoutInterval(3s) + MaxStatements(10000)`（Jint 4.x 是 Options 扩展方法，**不是** `LimitOptions` 属性）；共享 `CreateSandboxedEngine()`，JintExpressionEvaluator / DslSemanticValidator 一并接入。
  - endpoint 白名单：相对路径放行、`//host` 与未白名单绝对 URI 拦截，配置键 `CJDSL:Security:AllowedEndpoints`。
  - XSS 清洗：HtmlSanitizer 9.0.886，命名空间是 **`Ganss.Xss`**（不是 Gans.Http）。注意 JSON 深拷贝后 Props 值是 `JsonElement`，Sanitize 需兼容 string/JsonElement 两种。
  - 已集成到 `/generate`、`/generate-from-nlp`、`/adapt`（生成后先 Sanitize 再 Validate，不安全返 400 `Dsl.Security`）与 `/validate`（合并安全错误/警告）。
- **2.2 F 校验/渲染对齐**：DslSemanticValidator 收敛为 `_renderedComponentTypes`（~44 可渲染）+ `_experimentalComponentTypes`（实验性仅 Warning）；渲染器补 dialog/snackbar/markdown 三个 case。
  - markdown 用 **MudBlazor.Markdown 9.0.0**（用户选定），组件名是 **`<MudMarkdown>`**（不是 `<Markdown>`），命名空间在 MudBlazor 下；Web Program.cs 需 `AddMudMarkdownServices()` + `using MudBlazor;`。
- **2.3 B 事件闭环**：openModal（DslDialog.razor 承载，内容经 JSON 传递）/ closeModal（`context.DialogInstance?.Close()`）/ refresh（`OnRefresh` 回调）/ export（CSV + JS `CJDSL.downloadFile`，脚本注入 App.razor）。
  - MudBlazor 9.x 对话框实例是接口 **`IMudDialogInstance`**，且必须用 `[CascadingParameter]` 获取（不能 `@inject`）。
- **2.4 H 测试体系**：新建 `src/CJDSL.Tests`（xUnit 2.9.2 + Moq + FluentAssertions），10 个测试全过（安全 4 + 语义 3 + 模板生成 3）。sln 被进程锁死 `dotnet sln add` 失败，**改用 Edit 工具手写 sln 项目条目**（GUID `7A5D9B1C-...`）成功。
  - 注意：`GenerateOptions` 的命名空间是 `CJDSL.Domain`（文件在 ValueObjects 目录但命名空间不带后缀）。
- **2.5 G Dashboard**：TemplateDslGenerator.GenerateDashboardAsync 生成统计卡×4 + 图表卡 + 列表卡；LLM 路径沿用既有 fallback。

### 环境备忘（新增）
- 同一 `--artifacts-path` 目录**二次构建**会被残留进程锁定（FileListAbsolute.txt / rpswa 缓存 Access Denied）——每次构建换新目录最省事（本轮用了 cjdsl_p2~p6）。
- `CJDSL.sln` 被进程锁定无法用 dotnet CLI 写入，但 Edit 工具可以写入。

### 待办（Phase 2 收尾）
- 冒烟测试未做：5001 端口还跑着 Phase 1 的旧产物，重启服务需先杀残留进程（被安全策略拦截），需用户外部处理后再验证 dialog/export/markdown/安全拦截的实际运行效果。
