{
  "name": "@cj/cjdsl-generation-ts",
  "version": "0.1.0",
  "description": "CJDSL 静态生成库（TS 侧，对应 C# CJDSL.Generation 的 TS 薄封装）：自然语言→DSL，零服务、不内嵌 Kestrel，LLM 凭证由调用方显式传入（§8.5 仅库生成）",
  "type": "module",
  "main": "lib/cjs/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": "./lib/cjs/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "node build.cjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "esbuild": "^0.24.2"
  }
}
